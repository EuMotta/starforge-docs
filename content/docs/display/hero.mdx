---
title: Hero
description: Eye-catching hero section components for landing pages with gradient titles, promotional bars, and split layouts.
---

import { ComponentPreview } from '@/components/common/preview/component-preview';
import { extractSourceCode } from '@/lib/code';
import { Callout } from 'fumadocs-ui/components/callout';
import { status } from '@/constants';

<Callout title={status.tailwind_note.title}>
  All presented components are responsive and compatible with Tailwind CSS.
Make sure Tailwind is configured in your project to use them.
</Callout>

## Gradient Title with App Preview

A centered hero with a vibrant gradient text title, decorative blurred blob backgrounds, and a large app screenshot preview with light/dark mode variants. Includes a single call-to-action button and responsive typography scaling. Ideal for SaaS product launches and creative platform landing pages.

<ComponentPreview
  name="hero-1"
  classNameComponentContainer=""
  code={(await extractSourceCode('hero-1')).code}
  lang="tsx"
/>

---

## Business Landing with Promo Bar

A full business landing hero featuring a sticky promotional announcement bar, navigation header with logo and CTAs, a split text-and-image layout with trust badges (company logos), and dual call-to-action buttons (primary + outlined). Includes floating decorative corner shapes and a satisfaction score card overlay on the hero image. Perfect for corporate and B2B product sites.

<ComponentPreview
  name="hero-2"
  classNameComponentContainer=""
  code={(await extractSourceCode('hero-2')).code}
  lang="tsx"
/>

---

## Split Layout with Benefits List

A two-column hero with a benefits checklist (check icons), accent-colored heading highlight, and a large side image with a decorative glow/blur backdrop. The layout switches to single-column on mobile. Great for agency and consulting landing pages that need to emphasize value propositions upfront.

<ComponentPreview
  name="hero-3"
  classNameComponentContainer=""
  code={(await extractSourceCode('hero-3')).code}
  lang="tsx"
/>

