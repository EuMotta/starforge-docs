---
title: Select
description: Select
new: true
---

import { ComponentPreview } from '@/components/common/preview/component-preview';
import { extractSourceCode } from '@/lib/code';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { Callout } from 'fumadocs-ui/components/callout';
import { status } from '@/constants';

## Custom Select

Componente de seleção básico, sem integração com formulários.

<ComponentPreview
  name="select-1"
  classNameComponentContainer="min-h-[600px]"
  code={(await extractSourceCode('select-1')).code}
  lang="tsx"
/>

### Source Code

<DynamicCodeBlock
  lang="tsx"
  code={(await extractSourceCode('select-1')).sourceCode}
/>

## Custom Select (react-hook-form)

Componente de seleção integrado ao `react-hook-form` para gerenciamento de formulários.

<ComponentPreview
  name="select-1-form"
  classNameComponentContainer="min-h-[600px]"
  code={(await extractSourceCode('select-1-form')).code}
  lang="tsx"
/>

### Source Code

<DynamicCodeBlock
  lang="tsx"
  code={(await extractSourceCode('select-1-form')).sourceCode}
/>

### Como usar com React Hook Form

<Steps>
  <Step>
    **Instale as dependências**
    Certifique-se de ter o `react-hook-form` instalado:
    ```bash
    npm install react-hook-form
    ```
  </Step>
  <Step>
    **Configure o formulário**
    Importe o `useForm` do `react-hook-form` e configure o componente de seleção com o `FormField` para gerenciar o estado.
  </Step>
  <Step>
    **Integre o componente**
    Passe o `control` e o `formName` para o componente de seleção, conforme mostrado no código do exemplo `select-2`.
  </Step>
</Steps>

<Callout title={status.tailwind_note.title}>
  O componente com `react-hook-form` requer a configuração correta do `Controller` para funcionar adequadamente. Consulte a [documentação oficial do React Hook Form](https://react-hook-form.com/) para mais detalhes.
</Callout>