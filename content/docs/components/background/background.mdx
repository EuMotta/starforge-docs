---
title: Grid Background
description: A collection of customizable background components with different layouts.
---

import { ComponentPreview } from '@/components/common/preview/component-preview';
import { extractSourceCode } from '@/lib/code';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { Callout } from 'fumadocs-ui/components/callout';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { status } from '@/constants';

<Callout title={status.tailwind_note.title}>
  All presented components are responsive and compatible with Tailwind CSS. Make
  sure Tailwind is configured in your project to use them.
</Callout>

## Grid

<ComponentPreview
  name="background-1"
  classNameComponentContainer="min-h-[600px] w-full flex items-center justify-center"
  code={(await extractSourceCode('background-1')).code}
  lang="tsx"
/>

### Source Code

<DynamicCodeBlock
  lang="tsx"
  code={(await extractSourceCode('background-1')).sourceCode}
/>
